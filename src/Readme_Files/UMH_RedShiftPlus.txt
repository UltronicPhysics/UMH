UMH Redshift Simulation — README
================================

Purpose
-------
This simulation implements the Ultronic Medium Hypothesis (UMH) redshift model, in
which cosmological redshift is generated by tension in a pervasive medium rather
than by metric expansion. The code demonstrates that a *non-expansion* model can
fit the Pantheon+ supernova Hubble diagram while preserving standard time-dilation
of light curves.

Core UMH ingredients
--------------------
1) **Tension-driven redshift law**
   - Distance `d` → redshift `z` via
     [ z(d) = exp(α d) − 1,   L ≡ ln(1+z) = α d. ]
   - `α` is calibrated at low z from distance–redshift calibrators.

2) **Time-dilation and transfer function**
   - Physical time-stretch (light-curve dilation):  S(z) = (1+z)^δ.
     *UMH expectation:* δ = 1 (i.e., S = 1+z, same as SN Ia observations / GR).
   - Broadband spectral/photometric transfer function along the path:
     [ T(z) = exp( −β1 L − β2 L^2 ),  L = ln(1+z). ]
   - In the distance modulus, this enters as 1/√T(z) (i.e., an effective dimming/brightening).
   - Strategy: keep δ fixed at 1 (physical time stretch), and **profile** (fit) β’s to absorb
     the gentle broadband curvature required by the data. If β is *frozen to zero*,
     the fit compensates by drifting δ>1 — a useful diagnostic but not the preferred model.

3) **Luminosity distance and Hubble diagram (no expansion)**
   - Invert z→d under UMH (d = L/α). The model luminosity distance:
     [ D_L(z) = κ · d(z) · (1+z)^{(1+δ)/2} / √T(z). ]
     Here κ is an overall scale absorbed by the nuisance magnitude `M`.
   - Distance modulus:  μ(z) = 5 log10[D_L(z) / (1 Mpc)] + 25.

Data & fitting
--------------
• **Calibrators (low-z, SH0ES/Pantheon+ subset):**
  - We fit **distance vs. log-redshift** with an intercept:
        d = c0 + c1 * L,   where L ≡ ln(1+z), for z ≤ 0.10.
  - **Weights:** distance uncertainties, w = 1/σ_d²; solved with **Huber IRLS** (c ≈ 2.0).
  - We report **α ≡ a★ = 1/c1** and (for reference) **H₀ = c · α**.
  - Output files:
      • `UMH_RedShift_Calibration_Data.csv`
      • `UMH_RedShift_Calibration_Fit.json` (α, H₀, c0, c1, χ²/dof, N, and method flags).
  - *Note:* Earlier runs used a through-origin fit of L vs d at z ≲ 0.08 with σ_L weights; this README reflects the updated (Pantheon-matched) procedure.

• **Pantheon+ SNe (SN-only):**
  - Uses the provided STAT+SYS covariance; **M** is profiled (analytically minimized via Cholesky).
  - Two fits are contrasted:
      (A) **δ free, β fixed to 0**  → diagnostic; δ tends to drift > 1 to absorb curvature.
      (B) **δ = 1, β profiled (γ, β₂)**  → preferred UMH model.
  - Fit quality is reported as **χ²/dof**.

What to look for in the outputs
-------------------------------

Calibration figures
-------------------
1) **UMH_RedShift_Calibrators_z_vs_Distance.png**  
   - Shows local calibrators with errors, overlaid with:
     - UMH z(d) = exp(α d) − 1 (orange)
     - Linear Hubble relation z ≈ (H0/c) d (green dashed), with H0 = c α in the legend.
   - At low z these are indistinguishable; the data sit on the line.

2) **UMH_RedShift_ln1pz_vs_Distance.png**  
   - Calibration plot of **distance vs. log-redshift**: fit \(d = c_0 + c_1\,L\) with \(L \equiv \ln(1+z)\) (intercept **included**, robust Huber IRLS, \(z \le 0.10\)); we report \(\alpha \equiv a_\star = 1/c_1\).
   - Title shows **\(\alpha\)** and **\(H_0 = c \cdot \alpha\)** with uncertainties; annotations list \(c_0\), \(c_1\), \(N\), \(\chi^2/\mathrm{dof}\), and Huber \(c\).
   - The shaded band indicates the \(\approx 95\%\) confidence region of the fit.
   - This panel provides the one-time low-\(z\) calibration used throughout the run.

Hubble diagram figures
----------------------
3) **UMH_RedShift_Hubble_UMH_delta_vs_betas.png**  
   - Pantheon+ μ(z) points with two curves:
     - Solid: δ free, β=0 (diagnostic) → typically best δ ≈ 1.3.
     - Dashed: δ=1 with β profiled (preferred) → matches or *betters* the fit.
   - Message: broadband curvature belongs in T(z) (β), not in δ.

4) **UMH_RedShift_Hubble_UMH_delta1.png**  
   - Single-model display with δ=1 (physical time-stretch) using the profiled β’s.
   - Clean agreement across full redshift range (no expansion used).

Time-dilation / stretch figures
-------------------------------
5) **UMH_RedShift_Time_Stretch.png** (physical)
   - Plots S(z) = (1+z)^δ for:
     - Blue: δ = 1 (UMH expectation) — **overlaps the GR/SN-Ia dashed line S=1+z**.
     - Orange: best δ with β=0 (diagnostic; rises too fast at high z).
     - Green: δ implied by γ when β1 is fixed to 0 (also diagnostic).
   - A vertical guide at z_max can be shown to mark the dataset extent.

6) **UMH_RedShift_mu_timedilation_factor.png** (factor inside μ)
   - Plots (1+z)^{δ/2} vs z with the GR/SN-Ia guide (1+z)^{1/2} as dashed.
   - Again, δ=1 matches the guide; δ>1 diagnostic curves sit above it.

δ–scan diagnostic
-----------------
7) **UMH_RedShift_delta_scan_chi2.png**  
   - χ²/dof vs δ with β=0 (blue curve) — convex “U” with minimum near δ≈1.30.
   - Vertical dashed line at the best δ; a filled dot (in the overlay version
     `UMH_RedShift_delta_scan_beta_profiled_vs_fixed.png`) shows the δ=1, β-profiled χ²/dof.
   - Interpretation: **if β is fixed** the fit *forces* δ>1; **if β is profiled** you can
     keep δ=1 and reach comparable or better χ²/dof.

Residual diagnostics
--------------------
8) **UMH_RedShift_Residuals_mu_vs_z.png**  
   - μ_data − μ_model vs z for the two cases (“δ free, β=0” and “δ=1, β profiled”);
     a running median for δ=1 hovers around zero. No large-scale trend with z.

9) **UMH_RedShift_Residuals_Hist_beta_profiled.png**  
   - Residual distribution for the preferred δ=1, β-profiled fit with a normal overlay.
     Typical width ≈ 0.16 mag; small skewness is acceptable for Pantheon+.

10) **UMH_RedShift_Residuals_Binned_beta_profiled.png**  
    - Equal-N binned medians ±68% vs z for δ=1, β profiled — flat around zero across the range.
    - Optional per-bin counts are printed in small grey text above the axis.

Key outputs / numbers to cite
-----------------------------
• **Calibration (this run):** α = (2.481805 ± 0.00000772) × 10⁻⁴ Mpc⁻¹  →  H₀ = 74.40 km s⁻¹ Mpc⁻¹  
  (z ≤ 0.10, with-intercept, Huber IRLS c=2.0, weights 1/σ_d², N=77, c₀ ≈ +1.337 Mpc, χ²/dof ≈ 0.340; see log/JSON). 

• **Pantheon+ fits:**  
  – With **β=0** (diagnostic), best δ ≈ 1.300, χ²/dof ≈ 0.902. :contentReference[oaicite:1]{index=1}  
  – With **δ=1** and **β profiled** (γ, β₂), χ²/dof = 0.898, and residuals are flat with z. :contentReference[oaicite:2]{index=2}

What this demonstrates for UMH
------------------------------
1) A **non-expansion** redshift law (UMH z(d)) calibrated locally reproduces the Pantheon+
   Hubble diagram across the full redshift range.  
2) **Physical time-dilation remains δ=1** (S = 1+z), consistent with supernova light-curve
   stretching and GR expectations.  
3) The gentle broadband curvature demanded by the data is captured by **T(z)** via the β’s
   (a path-integrated transfer), not by altering δ.  
4) Residuals show **no systematic trend with redshift** once β’s are profiled, confirming
   the internal consistency of the UMH treatment.

Knobs & reproducibility 
-----------------------
- Low-z calibration window: `zmax` (default 0.10) and `dmax` (default 120 Mpc).
- Intercept toggle: `force_through_zero` (default False → fit d = c0 + c1·L).
- Robust regression: Huber IRLS `huber_c` (default 2.0), `max_iter` (default 50).
- Measurement weights for α: distance-domain, `w = 1/σ_d^2` (default).
- PV floor (σ_L): default 250 km/s — used only in L-domain diagnostics/plots, **not** in the α fit.
- Model profiling: choose to **profile β’s** (preferred) or **fix β=0** (diagnostic δ-scan); `M` is profiled.
- Reproducibility: the calibration JSON records `c0`, `c1`, α (=1/c1), H₀, χ²/dof, N, and all method flags; 
  all plots and the calibration CSV/JSON are written under the run’s output prefix.

Suggested captions to reuse in the paper
----------------------------------------
• “Green/orange curves are diagnostics with β fixed; the preferred physical model is δ=1 with profiled β.”  
• “A vertical dotted line marks the maximum redshift of the Pantheon+ SN sample used here.”

File list
-------------------
- UMH_RedShift_Calibrators_z_vs_Distance.png  
- UMH_RedShift_ln1pz_vs_Distance.png  
- UMH_RedShift_Hubble_UMH_delta_vs_betas.png  
- UMH_RedShift_Hubble_UMH_delta1.png  
- UMH_RedShift_delta_scan_chi2.png  
- UMH_RedShift_delta_scan_beta_profiled_vs_fixed.png  
- UMH_RedShift_Residuals_mu_vs_z.png  
- UMH_RedShift_Residuals_Hist_beta_profiled.png  
- UMH_RedShift_Residuals_Binned_beta_profiled.png  
- UMH_RedShift_Time_Stretch.png  
- UMH_RedShift_mu_timedilation_factor.png  
- UMH_RedShift_Calibration_Data.csv  
- UMH_RedShift_Calibration_Fit.json
